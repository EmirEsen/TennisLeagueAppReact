export enum TournamentPrivacy {
    PUBLIC = 'PUBLIC', // anyone on or off Tennis Club
    MEMBERS_PRIVATE = 'MEMBERS_PRIVATE', // Only tennis club members can view
    PRIVATE = 'PRIVATE', // ONLY participants can view the tournament
    USER_NETWORK = 'USER_NETWORK' // In the future, only user friends can view the tournament
}